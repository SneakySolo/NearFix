spring:
  application:
    name: nearfix

  datasource:
    url: jdbc:postgresql://localhost:5432/nearfix
    username: postgres
    password: 2285
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  thymeleaf:
    cache: false

app:
  upload:
    dir: D:/nearfix/uploads/