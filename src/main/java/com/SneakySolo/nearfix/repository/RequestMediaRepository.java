package com.SneakySolo.nearfix.repository;

import com.SneakySolo.nearfix.entity.RequestMedia;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RequestMediaRepository extends JpaRepository<RequestMedia, Integer> {
}
